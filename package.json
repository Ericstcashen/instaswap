{
  "name": "instaswap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trade Instagram handles in one scan. Self-hosted, own backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
